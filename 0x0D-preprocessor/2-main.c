#include <stdio.h>
/**
 * main - Program compilation starts from the main
 * Description: A program that prints the name of the file
 *
 * Return: 0 (success)
 */
int main(void)
{
		printf("%s\n", __FILE__);
			return (0);
}
