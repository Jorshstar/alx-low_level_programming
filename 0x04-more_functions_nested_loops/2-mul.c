#include "main.h"
/**
 * mul - A function multiplies two integers
 * @a: first integer
 * @b: second integer
 * I just wanna multiply this integers and be done with it
 * Return: returns result
 */
int mul(int a, int b)
{
	return (a * b);
}
